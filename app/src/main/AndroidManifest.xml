<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mktul_movil">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Producto_Catalogo"></activity>
        <activity android:name=".Catalogos" />
        <activity android:name=".CarritoCompras" />
        <activity android:name=".VendedorVentas" />
        <activity android:name=".AdminVendedores" />
        <activity android:name=".Categorias" />
        <activity android:name=".Config_Vendedor" />
        <activity android:name=".Config_Admin" />
        <activity android:name=".Config_Cuenta" />
        <activity android:name=".Registro" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/iniciomarketul">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>